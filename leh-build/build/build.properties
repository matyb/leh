dist.dir=dist
build.dir=build
bin.dir=build/bin
reports.dir=${build.dir}/reports
src.dir=src
src.bin.dir=${bin.dir}/${src.dir}
shared.dir=../shared/src
src.bin.dir=${bin.dir}/${src.dir}
test.dir=test
test.bin.dir=${bin.dir}/${test.dir}
jar.name=leh
shared.jar.name=leh-shared